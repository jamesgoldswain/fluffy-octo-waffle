import { css } from '@emotion/core';
import { ITheme } from '../../lib/theme';

export const styles = (theme : ITheme) => {
  return css`
  p {
    margin: 0;
  }

  button {
    padding: 10px;
    margin: 5px;
  }

`;
};
